{
  "name": "settleline",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "npx serve .",
    "anchor:build": "anchor build",
    "anchor:test": "anchor test",
    "anchor:deploy:devnet": "solana config set -ud && anchor deploy",
    "backend": "npm --prefix backend run dev"
  },
  "devDependencies": {
    "@coral-xyz/anchor": "^0.32.1",
    "@solana/web3.js": "^1.98.0",
    "@types/chai": "^4.3.16",
    "@types/mocha": "^10.0.9",
    "chai": "^4.5.0",
    "typescript": "^5.6.0",
    "ts-mocha": "^10.0.0"
  }
}
